name: flutter_crud_app
description: Application CRUD avec JSONPlaceholder - niveau débutant

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0                    # Appels API HTTP (GET, POST, PUT, DELETE)
  shared_preferences: ^2.2.2      # Cache local (stockage JSON en local)
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
